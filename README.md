# stsnp
STSNP is a tool for launching snap applications on Linux systems, especially designed for minimal wm like Openbox. The application accepts arguments and allows you not to use them, so far only CLI, but GUI is planned.
